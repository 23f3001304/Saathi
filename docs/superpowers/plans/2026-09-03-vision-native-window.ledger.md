# SDD ledger — plan: docs/superpowers/plans/2026-09-03-vision-native-window.md
BASE b552eb3ffb19a2d6375a9a7265b2ae7aa8e116d5 (master)
Pre-flight scan: Task1 (browser-drive caster/session, agent-host frame-*, audit-ui browserFrames) and Task2 (agent-host web-handover*, web-buy-tools, web-tool-runner, web-buy-errand; agents money-tool-registry) share no files → dispatched in parallel with path-scoped commits. Task3/4 both touch web-verify.ts + web-tool-runner.ts + turn-plan-*.ts → sequential after 1+2. Task5 touches web-tool-runner.ts/web-buy-tools.ts after 2/3. Task6 audit-ui only. Task7 headless-reader.ts after Task3's BatchRead change.
Task 1 (I) and Task 2 (J): dispatched in parallel (Opus each) from BASE b552eb3.
2026-09-03 crash recovery: controller session died after the parallel dispatch; neither implementer reported or committed. Working tree at b552eb3 held Task 2's uncommitted work (15 modified + web-handover-move.ts + its test; 46/46 tests green in web-handover-move/web-tools/web-bot-check/web-pick) and Task 1's Step-1 reproduction only (container-cast-navigation.test.ts + fixtures cast-a/cast-b, two TS2532 errors, no src change).
Ruling: resume SEQUENTIALLY, Task 2 first then Task 1 — parallel implementers share git index, tsc -b dist outputs and the container name; the crash left both half-done with no report — cost if wrong: one task's wall-clock added to the pair.
Ruling: Task 1's three untracked files parked at .superpowers/sdd/2026-09-03-vision-native-window/parked-task1/ (fixtures/cast-a.html, fixtures/cast-b.html, tests/container-cast-navigation.test.ts) so Task 2's tsc -b gate is clean; RESTORE them to packages/browser-drive/fixtures/shop/ and packages/browser-drive/tests/ before dispatching Task 1 — cost if wrong: Task 1's implementer redoes ~40 lines of test.
Task 2: re-dispatched (fresh Opus implementer, owns the prior uncommitted work) from BASE b552eb3.
Task 2: implemented at fb29a5e (DONE_WITH_CONCERNS: HandoverMove 96 prettier lines vs brief's ≤80; web-steps.ts + web-phase.test.ts touched as consequences; web-challenge.ts deleted, botCheck folded into observeWindow). Mapping shipped: payment→payment, sign-in→login, human-check→captcha, other→final-review. Review package review-b552eb3..fb29a5e.diff; reviewer (Opus) dispatched.
Task 2: review of b552eb3..fb29a5e — spec ✅, quality Approved, 1 Important: web-bot-check.test.ts:34-40 "attempts nothing at all" is vacuous (passes because CHECKPOINT fixture has no search box, not because the floor holds); challenge-page floor (RelayGate refusing the opaque widget) undemonstrated anywhere in the suite.
Task 2: minor (deferred): web-handover-move.ts is 96 prettier lines vs brief's "≤ 80" — Ruling: the target stands as a target, not a gate; the same brief sentence mandates moving 24 lines of stop copy into the file and the enforced limit (200) holds — cost if wrong: one split of SAYS into its own file later.
Task 2: minor (deferred): web-address-confirm.test.ts measures 208 prettier lines (was 202 at HEAD; 199 on disk so eslint is green); web-pick.test.ts 205, line-neutral in this diff.
Task 2: minor (deferred): handover reasons enumerated in four places (HandoverReason type, RAISES, webHandoverArgs, inline z.enum in the declaration) — only RAISES is compiler-tied; schemaOf(webHandoverArgs.shape) would tie the declaration to the parse schema.
Task 2: minor (deferred): no fake-shop page carries a "Buy Now" button, so spec §9 "a product page with a Buy Now button does not hand over" is proved only as a direct observeWindow call, not through the surface.
Task 2: minor (deferred): web-handover-move.ts:78,82 wraps synchronous work in Promise.resolve; raise() could return WebResult.
Task 2: minor (deferred): web-handover-move.ts:76-77 records progress.handedOver BEFORE window.handoff().raise(); if raise throws SessionStateError, progress claims a handover the wheel never made (same order as the deleted stop(); swap is free).
Task 2: fix round 1/5 dispatched (resume original implementer) for the vacuous bot-check test; FIX_BASE fb29a5e.
Task 2: fix round 1/5 fix landed at 0d6d9ba (bot-check fixture given a real challenge label; refusal asserted through FieldClassifier); scoped re-review (Sonnet) dispatched on fb29a5e..0d6d9ba.
Task 2: fix round 1/5 (1 addressed, 0 open — vacuous bot-check test now asserts the classifier refusal captcha_context/handoff captcha through the real stack; commits fb29a5e..0d6d9ba)
Task 2: minor (deferred): the RelayGate half of the challenge floor is not demonstrated in apps/agent-host because FakeShopPage.describeAt() returns null on every page (pre-existing fixture limit; named in the test header, not papered over).
Task 2: complete (commits b552eb3..0d6d9ba, review clean)
Task 1: parked files restored from parked-task1/ to packages/browser-drive/{fixtures/shop,tests}/; dispatching (Opus, debugging task) from BASE 0d6d9ba.
Ruling: Task 1 may rebuild covenant-browser-sandbox:latest (Dockerfile:121 bakes fixtures/shop into /opt/covenant/fixtures/shop, so the two full-bleed cast fixtures are invisible to the container until rebuilt) — the plan calls for the reproduction against the real image and the change is two static HTML files; the founder's live agent-host launches new sandboxes from the tag — cost if wrong: one broken sandbox launch for the founder until the image is rebuilt again (the container suite itself exercises the image before anything else does).
Task 1: implemented at 7b3adce (DONE_WITH_CONCERNS: cause = screencast CDP session stays bound to the document it started on and Chrome keeps compositing it after the next commit, none of (a)(b)(c); concerns: PuppeteerCaster/MainFrameNavigations hold the constructor Page which freshPage() can replace; ~400ms hole across navigation untouched; fake-sandbox.ts split into fake-sandbox-session.ts; browser-session.ts/browser-service.ts at 199 lines; image rebuilt via docker compose --profile build-only). Review package review-0d6d9ba..7b3adce.diff; reviewer (Opus) dispatched.
Task 1: review of 0d6d9ba..7b3adce — spec ❌ (§6b clause 3, the per-second "subscriber went away" restart loop, closed by reassignment not evidence), 2 Important: (1) main-frame-navigations.ts counts same-document navigations (pushState/hash) because puppeteer-core 25.9.0 emits framenavigated for both, so every live-session frame is dropped for ~400ms on any SPA-style shop; (2) puppeteer-page.ts binds the counter and the caster to the constructor Page while live()/freshPage() swaps this.page on a cross-process navigation, freezing navigations() — the founder's moglix→amazon case.
Task 1: Ruling on §6b clause 3 — the FIX of the teardown loop belongs to Task 6 per the plan (spec §6 G: torn down only on session close or card unmount; a visibility rule pauses painting, not the subscription), but Task 1 owes the FINDING: candidate (c) was never tested (the container reproduction has no client, so it cannot rule (c) out); the fix round must reproduce the client half, name what tears the subscriber down with the line/log that proves it, fix it here if it is the EventSource dying on a healthy stream (brief Step 5), and hand it to Task 6 if it is the visibility/teardown rule — cost if wrong: Task 6 inherits a mis-attributed cause and the loop survives one more pair.
Task 1: Ruling on ⚠️ clause 5 (click echo and stream never disagree) — resolved by construction: the echo is a fresh stamped capture after the click and the stream's pre-commit frames are dropped by the sink; no test asserts it — deferred minor for Task 6 (audit-ui) — cost if wrong: one untested client-side disagreement path.
Task 1: Ruling on ⚠️ clause 1 (poll path answers NO_FRAME when its fresh capture straddles a commit, instead of re-capturing) — intended: the next SERVED frame is of the new URL, which is what §6b says; re-capturing in the route could loop across a slow commit — cost if wrong: the card holds its last frame ~one poll interval longer.
Task 1: minor (deferred): container-cast-navigation.test.ts can pass vacuously on a run where Chrome delivers no post-commit frame of the old document; assert at least one stale frame recognised across the A→B→A→B walk to make it a regression guard.
Task 1: minor (deferred): staleness rule written three times (frame-sink.ts:101-107, window-routes.ts:41-49, the container test); the two production copies could share one predicate over Capture.
Task 1: minor (deferred): frame-fakes.ts casts FakeFeedService through unknown to FeedService; a structural { navigations(): number } parameter on newFeed would remove the cast.
Task 1: minor (deferred): browser-session.ts and browser-service.ts sit at exactly 199 lines (zero headroom for Tasks 5/7).
Task 1: minor (deferred): browser-look.ts:49-56 swallows a throw from session.navigations() into 0 with no log line.
Task 1: fix round 1/5 dispatched (resume original implementer): Important 1, Important 2, spec gap clause 3 (find (c)); FIX_BASE 7b3adce.
Task 1: fix round 1/5 fix landed at 1eadf2d (count on the CDP wire deduped by loaderId; PuppeteerCaster.follow(page) after freshPage() swaps; (c) ruled out with test output — teardown is useBrowserSession's effect cleanup on conversationId change / unmount, Windows.tsx:59-71, per-second cadence not reproduced); scoped re-review (Opus) dispatched on 7b3adce..1eadf2d.
Task 6 carry-in (from Task 1 fix report): the per-second subscriber teardown is NOT the stream — (c) ruled out (one socket opened across four state-read cycles, never closed). It is useBrowserSession's effect cleanup (attach → startWatch → stop(wire)) re-running when conversationId changes or the pane unmounts: Windows.tsx:59-60 derives conversationId from an unordered lanes[0] on a 3 s poll, and Windows.tsx:61-71 unmounts the pane on an empty lane list. Cadence of 1 s not reproduced offline. Task 6 owns the fix (spec §6 G teardown rule).
Task 1: fix round 1/5 (3 addressed, 0 open of the original three; commits 7b3adce..1eadf2d) — NEW Important in the fix diff: browser-stream-life.test.ts:113,160 returns a spread copy so live.polls is a detached zero and "shutter never needed" can never fail.
Task 1: minor (deferred): puppeteer-page.ts:68 / puppeteer-caster.ts:79 — follow() on the swap path is unguarded where attach() is caught; a rejecting createCDPSession/Page.enable on the fresh page throws at live() and leaves the cast dead and the counter frozen (machinery silently off).
Task 1: minor (deferred): main-frame-navigations.ts:54-73 and puppeteer-caster.ts:72-83 — follow() has no re-entrancy guard; two concurrent live() recoveries leak the predecessor's CDP session; the old counter session's Page.frameNavigated handler is never off()'d before detach, so a commit in that window increments spuriously.
Task 1: minor (deferred): puppeteer-launcher.ts:53 — await PuppeteerPage.open(page) can throw inside launch() with no try/catch, leaking the Chrome process (container path is covered).
Task 1: minor (deferred): a pushState still costs a coalesced cast reattach (~400 ms delivery hole) because puppeteer-caster.ts:48-56 onNavigated listens on the page-level framenavigated; frames are no longer discarded, the freeze is not removed.
Task 1: fix round 2/5 dispatched (resume original implementer): inert live.polls assertion; FIX_BASE 1eadf2d.
Task 1: fix round 2/5 fix landed at 49f2e60 (host.server filled in after the handler closes over the object; RED/GREEN with the spread copy shown vacuous); scoped re-review (Sonnet) dispatched on 1eadf2d..49f2e60. minor (deferred): browser-frames.test.ts:88 has the same spread in its own start() — nothing vacuous yet.
Task 1: fix round 2/5 (1 addressed, 0 open — live.polls now read off the same host object the handler mutates; commits 1eadf2d..49f2e60)
Task 1: complete (commits 0d6d9ba..49f2e60, review clean)
Pair I+J on master at 49f2e60 → restarting agent-host (container sandbox) so the founder can test.
agent-host restarted at 49f2e60: old pid 101972 stopped, new pid 124240 listening on :8788 (COVENANT_BROWSER_SANDBOX=container, log C:/Users/coehe/AppData/Local/Temp/claude/C--Users-coehe-Razorpay/826f9b8e-8c42-42a1-8ceb-91af75af7008/scratchpad/agent-host-49f2e60.log). Task 3: dispatching (Opus) from BASE 49f2e60.
Task 3: implemented at dcd367c (DONE_WITH_CONCERNS: failureOf moved runner→web-tool-guards.ts (runner at 200/200); verify+card grouped as one research:{verify,card} constructor param (complexity); web-buy-tools.ts now 200/200 — Task 5's web_scroll declaration needs RESEARCH/SHOP declarations split out; VerifiedReads keyed on settled URL so a pre-redirect URL row is refused url_not_verified; web_found now declared to no model — dead path). Review package review-49f2e60..dcd367c.diff; reviewer (Opus) dispatched.
Task 3: review of 49f2e60..dcd367c — spec ❌: (1) image_url provenance check (spec §2: https: AND in the page's declared or tile images, else the woven mark) not built — the model's string reaches the card via web-card.ts:84,112 with only the scheme checked; (2) rows bound 1..8 in both schema copies, spec says 1..6; (3) VerifiedPage.declared is "declared or first tile" (web-verify.ts pageOf uses dom.listings[0] from mergeListings), spec says the schema.org product if any; (4) web-card.ts 154 lines vs brief ≤150. Important: price-probe.ts:28 carries an undisclosed eslint-disable max-lines-per-function on a Global-Constraints hard limit, reported as LINT_OK.
Task 3: Ruling on (1) — the spec binds; the plan's CardVerbs interface omitted the image clause; build it: accept image_url only when https: and present among the page's declared image(s) or tile images (VerifiedPage must carry the tile images for that), otherwise card the row WITHOUT the image (woven mark) — a bad image is not a refusal — cost if wrong: one extra field on the verify row.
Task 3: Ruling on (4) — ≤150 is a target, the enforced gate is 200; because the image check grows the file, the implementer MAY extract the image provenance check into web-card-image.ts (≤40 lines) if web-card.ts would pass 150 — cost if wrong: one small extra module.
Task 3: Ruling on the eslint-disable — Global Constraints are hard limits with no suppression path; restructure the probe so no function exceeds 40 lines (follow headless-reader.ts's in-page read pattern; two evaluates if needed) — cost if wrong: one slower batch read.
Task 3: minor (deferred): price-probe.test.ts:65 describe.skipIf reads `skip` before beforeAll assigns it — the guard can never fire; without Chrome every test throws instead of skipping.
Task 3: minor (deferred): price-probe.ts:51-60 `around` searches the collapsed parent text for the UNcollapsed candidate; a price with an interior newline gets the parent's head as context; collapsing in owns() would also align prices[].text with BatchRead.text.
Task 3: minor (deferred): web-card.ts:147-155 named() accepts "" (text.includes("") is true); caught later as url_not_verified — a wrong reason.
Task 3: minor (deferred): web-card.ts:70 `?? "url_not_verified"` for a null mint is a wrong reason; a "not_recorded" member would be honest.
Task 3: minor (deferred): no stepLabel/LABELS entry for web_card, and pillFor was trimmed to "Read {shop}" — the model's naming step emits no pill (shopper-visible).
Task 3: minor (deferred): the card row schema is duplicated (web-buy-tools.ts cardRow vs web-tools.ts webCardRow) per the file's convention; one export would tie them.
Task 3: minor (deferred): spec §8's two founder pages are covered as unit facts, not as end-to-end fixtures; the sign-in-wall case has no host check behind it by design, so a fixture test is the only place that design is exercised.
Task 3: minor (deferred): BatchRead.priceText removed (spec said "gains"); no other reader found.
Task 3: minor (deferred): no test for two rows naming the same URL (record does not dedupe; since() does).
Task 3: fix round 1/5 dispatched (resume original implementer): image_url provenance, rows 1..6, declared-only, no eslint-disable, ≤150 or extract; FIX_BASE dcd367c.
Task 3: fix round 1/5 fix landed at 64bfb94 (BatchRead.declared separate; VerifiedPage.images; web-card-image.ts 23 lines; rows 1..6 both copies; declared null on tiles-only pages; probe split scanPrices in-page 33 lines + rankPrices host-side, no disables; web-card.ts 148; verify cases moved to new web-verify.test.ts); scoped re-review (Opus) dispatched on dcd367c..64bfb94.
Task 3: fix round 1/5 (5 addressed, 0 open; commits dcd367c..64bfb94)
Task 3: minor (deferred): web-verify.test.ts:82-85 lost the end-to-end assertion that CardVerbs cards a page pageOf() actually produced (was in web-card.test.ts at dcd367c); web-card.test.ts's readOf() is a hand-written literal.
Task 3: minor (deferred): web-verify.test.ts:56,74,80 constructs a WebFindings never handed to VerifyVerbs, so expect(findings.length).toBe(0) cannot fail.
Task 3: minor (deferred): listing-script.ts:19 carries a pre-existing eslint-disable max-lines-per-function on readDeclaredListings (same class the round ruled out; untouched file).
Task 3: complete (commits 49f2e60..64bfb94, review clean)
Task 4: dispatching (Opus) from BASE 64bfb94. Carry-in: turn-plan-tools.ts 200/200 and web-look-step.ts 205 prettier lines — splits authorized; web-buy-tools.ts 200/200 (Task 5 carry-in).
Task 4: implemented at 7bb91a6 (DONE_WITH_CONCERNS: shop folded into WebPin.allows() so a later pick errand's web_open is held to the last look's shop with offPin's product-shaped sentence; TurnPlan.shop is string|null mirroring query; turn-plan-record.ts 200/200; splits used: turn-plan-web-move.ts, turn-plan-move-rule.ts; pin/refusal cases in new web-pin.test.ts because web-look/web-card tests are at 197). Review package review-64bfb94..7bb91a6.diff; reviewer (Opus) dispatched.
Task 4: review of 64bfb94..7bb91a6 — every literal value verbatim; spec ❌ on one clause: the shop was folded into WebPin.allows() (web-pin.ts:379-384) whose SOLE production consumer is web_open in the buy errand (web-tool-runner.ts:167), so a pick errand after a look that named a shop is refused off-host with offPin's false "That is a different product" sentence; hold()/release() have no production callers. Important #1, labeled plan-mandated (the plan's Produces line said forShop → allows amazon.in).
Task 4: Ruling on Important #1 — spec §3 binds the pin to web_verify and web_card ("pins the errand: web_verify and web_card refuse"); the plan's "allows" wording argued the WebPin SHAPE, not a gate on web_open. NARROW: drop the shop from allows(); both research verbs already call offShop directly; add a test that a named shop does not gate web_open — cost if wrong: a buy errand could open a different shop than the one named, but the buy errand's integrity seam is the tapped product, not the shop.
Task 4: ⚠️ resolved — web-look-step.ts at 205 prettier lines and turn-plan-record.ts at 200 are known pre-existing carry-overs (ledgered before Task 4's dispatch), unchanged in substance by this diff.
Task 4: minor (deferred): web-pin.ts:87-91 release() is dead code after this diff (only listing-identity.test.ts calls it).
Task 4: minor (deferred): web-pin.ts:394-397 the dot rule shadows the table — "reliance.digital" resolves to a nonexistent host and shopNote reports it as resolved; trying the table first would be behaviour-identical for every known name and more robust.
Task 4: minor (deferred): web-pin.test.ts:643-649 module-level `asked` never reset — the verify assertions are order-dependent.
Task 4: minor (deferred): web-verify.ts:171-174 skips read() when every URL is off-shop, so no step pill fires for the refused-everything call.
Task 4: minor (deferred): web-pin.test.ts lands at 196/200 lines (new file, no headroom).
Task 4: observation for the final review (pre-existing, out of scope): WebPin.hold() has no production caller — the buy errand's product pin appears unwired; allows() was inert in production before this task.
Task 4: fix round 1/5 dispatched (resume original implementer): narrow allows(); FIX_BASE 7bb91a6.
Task 4: fix round 1/5 fix landed at a0612d6 (allows() is the product question again; offShop gates the two research verbs; DECISION paragraph; new allows() case; web-pin.test.ts 199/200); scoped re-review (Sonnet) dispatched on 7bb91a6..a0612d6.
Task 4: fix round 1/5 (1 addressed, 0 open; commits 7bb91a6..a0612d6)
Task 4: complete (commits 64bfb94..a0612d6, review clean)
Pair D+H on master at a0612d6 → restarting agent-host (container sandbox) so the founder can test.
agent-host restarted at a0612d6: old pid 124240 stopped, new pid 128764 listening on :8788 (COVENANT_BROWSER_SANDBOX=container, log C:/Users/coehe/AppData/Local/Temp/claude/C--Users-coehe-Razorpay/826f9b8e-8c42-42a1-8ceb-91af75af7008/scratchpad/agent-host-a0612d6.log). Task 5: dispatching (Opus) from BASE a0612d6. Carry-in: web-shopper.ts 199, web-tool-runner.ts 199, web-buy-tools.ts 200 prettier lines — one split each authorized; beat-rehydrate.test.ts lives in apps/audit-ui/tests.
Task 5: implemented at ebffe22 (DONE: extras beyond brief — pictureOf refuses to capture unless agent-drive (FrameCapture stops redacting in user-drive), and the capture has its own 10 s ceiling outside withinCall; web_search gets no picture (not on the list); vision tests in new web-vision.test.ts; fake-session builder moved to web-doubles.ts; web_sign_in/web_enter_code proved via unknown_tool outcomes since the harness wires no vault). Review package review-a0612d6..ebffe22.diff; reviewer (Opus) dispatched.
Task 5: review of a0612d6..ebffe22 — §5/§8 clauses present (WINDOW_MOVES exactly nine; SEEING byte-for-byte; both fallback bullets gone; RESUME gains the paragraph via parts.errand); two unlisted deviations; 1 Important: web-glance.ts glance() maps only NO_PICTURE to the no_picture failure, so SHUTTER_CLOSED / WINDOW_IS_THEIRS fall through to glanced() returning ok:true, 0×0, and a note promising an image that is null — a regression from the previous no_picture failure, reachable when a classifier refusal moves the wheel mid-call; the vision tests assert only picture/image.
Task 5: Ruling — pictureOf(session, waiter) accepted over the brief's pictureOf(session): BrowserSession exposes no waiter; injecting it into GlanceVerbs matches the settledRead/SignInVerbs precedent — cost if wrong: one signature differs from the plan text.
Task 5: Ruling — PointActions.scroll (point-actions.ts, not in the brief's file list) accepted: UserInput.scroll is user-drive-gated, so an agent-gated mirror with assertAgentMayAct("scroll") and a page.scrolled journal line is the correct seam — cost if wrong: one new browser-drive port surface the final review must see.
Task 5: Ruling on ⚠️ — the shutter is not in force in production: frame-capture.ts:48-71 is a standing DECISION that nothing produces a Blackout any more; under agent-drive a focused password field yields a painted, REDACTED frame. Pre-existing (LLM-native rebuild), not this task's. The seam is implemented and tested here. Spec §5's shutter clause stays as the behaviour of the seam when a blackout occurs; the guarantee in force is redaction. Surface to the founder in the final rulings list — cost if wrong: the spec overstates a guarantee.
Task 5: verified by reviewer — the agent-drive gate in pictureOf is an integrity necessity (frame-capture.ts:137-138 short-circuits to yours(), which returns redacted: 0 in user-drive); the 10 s capture ceiling is proportionate (withPicture runs after bounded()).
Task 5: minor (deferred): dy: 0 accepted and untested (pays the settle + beat for a no-op).
Task 5: minor (deferred): web_sign_in/web_enter_code/web_press{x,y} carry the picture only through refusal outcomes (harness wires no vault; describeAt null) — a vault-wired harness variant would close it.
Task 5: minor (deferred): no test pins the SEEING paragraph or the removed bullets (verified by hand twice).
Task 5: minor (deferred): BUY says "Say nothing between moves." (SEEING) and then "Say nothing while you work…" — doubled instruction.
Task 5: minor (deferred): web-act-calls.ts doc comment wrap leaves a two-word orphan line.
Task 5: minor (deferred): web-picture-call.ts:905-913 noted() would spread an array body (theoretical; Array.isArray guard).
Task 5: minor (deferred): report lists web-doubles.ts at 120 lines; it is 126.
Task 5: fix round 1/5 dispatched (resume original implementer): web_glance withheld → no_picture failure carrying the note; FIX_BASE ebffe22.
Task 5: fix round 1/5 fix landed at 8ee52d5 (glance() branches on seen.image === null → blind(note) returns the no_picture failure carrying the note; glanced() only with an image); scoped re-review (Sonnet) dispatched on ebffe22..8ee52d5.
Task 5: fix round 1/5 (1 addressed, 0 open; commits ebffe22..8ee52d5)
Task 5: complete (commits a0612d6..8ee52d5, review clean)
Step F on master at 8ee52d5 → restarting agent-host (spec §10: each step restarts so the founder tests live; Task 6 is audit-ui only, Task 7 restarts again).
agent-host restarted at 8ee52d5 (log C:/Users/coehe/AppData/Local/Temp/claude/C--Users-coehe-Razorpay/826f9b8e-8c42-42a1-8ceb-91af75af7008/scratchpad/agent-host-8ee52d5.log). Task 6: dispatching (Opus) from BASE 8ee52d5.
Task 6: implemented at e7d3aa9 (DONE: native non-passive wheel; teardown cause fixed in Windows.tsx — shown lane ordered and latched, one empty lanes poll not believed, proven by new window-lane-hold.test.tsx; four strip lines; horizontal wheel prevented but relays nothing (DECISION); concern: a host that truly goes away empties the Windows screen in ~6 s instead of ~3 s — fetchLanes cannot tell 'no windows' from 'poll failed'). Review package review-8ee52d5..e7d3aa9.diff; reviewer (Opus) dispatched.
Task 6: review of 8ee52d5..e7d3aa9 — spec ✅, Approved, 0 Important.
Task 6: ⚠️ resolved — "cast torn down only on close/unmount": the other useBrowserFeed dep (`active`) was shown by Task 1's remount test to cost nothing across same-lane re-renders; this diff removes the two Windows.tsx triggers Task 1 named. ⚠️ resolved — the step pills for a research errand with no window are untouched pre-existing behaviour; the strip half is proven by window-strip.test.tsx.
Task 6: minor (deferred): LiveViewport.tsx wheel ignores deltaMode (Firefox line mode relays ~3px per notch).
Task 6: minor (deferred): sub-pixel deltaY is prevented but relays nothing (gesture swallowed); accumulate a remainder or floor to ±1.
Task 6: minor (deferred): browser-sandbox.test.tsx proves preventDefault() is called, not that the listener is non-passive (jsdom cannot see passivity); spy addEventListener for { passive: false }.
Task 6: minor (deferred): window-lane-hold.test.tsx tests the hold but never the release (two consecutive blank polls bringing the pane down).
Task 6: minor (deferred): window-lane-hold.test.tsx uses ~7.5 s of real-clock sleeps with ~900 ms slack against the 3 s poll.
Task 6: minor (deferred): mixed global-stubbing styles in window-lane-hold.test.tsx (Reflect.set vs vi.stubGlobal).
Task 6: minor (deferred): Windows.tsx orders open windows by opaque conversation id (stable but arbitrary-looking); dead-defensive `?? ""`.
Task 6: minor (deferred): WindowStrip.tsx checks `launching` before the closed/null guard, so closed+launching still renders the opening line (pre-existing precedence; untested combination).
Task 6: observation for the final review: fetchLanes returns [] for an unreachable host as well as for "no windows", so a host that truly goes away empties the Windows screen in ~6 s instead of ~3 s.
Task 6: complete (commits 8ee52d5..e7d3aa9, review clean)
Task 7: dispatching (Opus) from BASE e7d3aa9. Carry-in: headless-reader.ts at 187 prettier lines — a split for the ReaderBrowser port + NativeReaderBrowser is authorized; container suites run alone and never while a covenant container is up.
Task 7: implemented at 5df729c (DONE_WITH_CONCERNS: DeferredReaderBrowser bridges sync buildRoot to the async Docker probe (adapter lives in browser-drive since agent-host has no puppeteer dep); ConnectedBrowser narrows ContainerLauncher's return so the reader gets the puppeteer Browser without leaking puppeteer into the shared port; headless-reader.ts 199/200; in-process reads now relaunch Chrome per batch (ends the old never-closed reader Chrome); buildRoot starts the Docker probe at boot; containerPlan/inProcessPlan exported for a deterministic plan test). Review package review-e7d3aa9..5df729c.diff; reviewer (Opus) dispatched.
Task 7: review of e7d3aa9..5df729c — spec ✅ clause by clause (shared plan, shared lockdown via assertContainerLocked + assertSandboxIntact, read_<hex>, no host Chrome under container, boot log once); 1 Important: one HeadlessReader is shared by every lane (composition-root.ts:159) and now holds a single stateful ReaderBrowser whose open() begins with close() on all three implementations, so two concurrent readMany calls destroy each other's browser and both batches return all-failure — a regression from the old process-long cached browser.
Task 7: Ruling on Important #1 — HeadlessReader takes the plan's factory `() => ReaderBrowser` (SandboxPlan.readerBrowser already IS a factory) and each readMany opens its own surface and closes it in a finally; spec §4 says "one container per batch", so one surface per batch is the truer shape and makes concurrent reads independent; the brief's constructor signature (a ReaderBrowser instance) changes — cost if wrong: in-process mode launches one Chrome per concurrent batch instead of sharing one.
Task 7: Ruling — headless-reader.ts is at 199/200 and the fix lands there: the BatchRead types may be cut into a sibling (e.g. chrome/batch-read.ts) re-exported from index.ts.
Task 7: ⚠️ noted — "in-process mode keeps today's launch": the call is byte-identical; the lifetime moved from process-long (never closed) to per-batch. Accepted: the old browser was never closed, and per-batch is what §4 describes for the container surface; the two surfaces now share one lifetime rule.
Task 7: minor (deferred): buildRoot now shells out to the docker CLI at construction (background probe, memoized, caught); pass the plan promise in if a later task needs buildRoot pure.
Task 7: minor (deferred): sandbox-plan.ts:128-132 logs window and reader from the same field, so the "both named" test is tautological.
Task 7: minor (deferred): ContainerBrowser.connection is now public — a widened seam; a DECISION note that it is for read-only surfaces would fence it.
Task 7: minor (deferred): HeadlessReader.close() is dead API once readMany closes per batch.
Task 7: minor (deferred): the bad-image container test asserts no leftover container but not no leftover network.
Task 7: fix round 1/5 dispatched (resume original implementer): per-batch surface via the factory; FIX_BASE 5df729c.
Task 7: fix round 1/5 fix landed at 3c463d3 (HeadlessReader takes () => ReaderBrowser; one surface per batch, one finally; DeferredReaderBrowser per batch; dead close() removed; headless-reader.ts 196, no split; new reader-batches.test.ts RED reproduced the trace; concern: no ceiling on concurrent reader browsers beyond the lane cap); scoped re-review (Opus) dispatched on 5df729c..3c463d3.
Task 7: fix round 1/5 (1 addressed, 0 open; commits 5df729c..3c463d3)
Task 7: observation for the final review: the reader launches one browser per concurrent batch with no ceiling beyond the lane cap; a batch-level concurrency limit may be worth a later task.
Task 7: complete (commits e7d3aa9..3c463d3, review clean)
ALL 7 TASKS COMPLETE at 3c463d3. Final whole-branch review: MERGE_BASE b552eb3 (the plan's BASE; the repo has no main and no remote), HEAD 3c463d3.
Final whole-branch review dispatched (Fable, most capable available) on review-b552eb3..3c463d3.diff with final-review-triage.md.
Final whole-branch review (Fable) STOPPED by the founder at pass 3/6; founder chose /code-review ultra as the final review instead. Ruling: agent-host restarted on 3c463d3 now (spec §10: each step restarts so the founder tests live) — cost if wrong: the founder tests a HEAD the cloud review may still fault.
