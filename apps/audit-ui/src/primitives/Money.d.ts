import type { JSX } from "react";
type MoneyProps = {
    paise: number;
    className?: string;
};
/** §6.4 — every rendered money amount goes through this component. */
export declare function Money({ paise: amount, className }: MoneyProps): JSX.Element;
export {};
//# sourceMappingURL=Money.d.ts.map