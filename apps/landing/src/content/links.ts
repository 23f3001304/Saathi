/**
 * DECISION: the two submission links do not exist yet (the five-minute
 * video is filmed on day three; the repo goes public at submission), and a
 * marketing page must not ship a dead href. Until the real URLs land here,
 * both CTAs walk the reader to the page's own nearest evidence: the demo
 * button to the shopping fragment, the build button to the hisaab. Swap
 * these two constants before publishing; nothing else changes.
 */
export const DEMO_URL = "#window";
export const BUILD_URL = "#hisaab";
